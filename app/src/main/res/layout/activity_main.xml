<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg">

    <!-- top spacer: Có thể loại bỏ nếu bạn quản lý window insets, nhưng giữ lại theo yêu cầu -->
    <View
        android:id="@+id/top_space"
        android:layout_width="0dp"
        android:layout_height="28dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/top_space">

        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="Lịch Học"
            android:textSize="18sp"
            android:textStyle="bold" />
    </androidx.appcompat.widget.Toolbar>

    <!-- Khu vực 2 card trên cùng -->
    <LinearLayout
        android:id="@+id/top_cards_container"
        android:layout_width="0dp"
        android:layout_height="170dp"
        android:orientation="horizontal"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Card bên trái -->
        <androidx.cardview.widget.CardView
            android:id="@+id/overview_card"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            app:cardCornerRadius="12dp"
            app:cardElevation="3dp">
            <!-- Nội dung cho Card trái ở đây -->
        </androidx.cardview.widget.CardView>

        <!-- Khoảng cách giữa 2 thẻ -->
        <View
            android:layout_width="8dp"
            android:layout_height="match_parent"/>

        <!-- Card dọc bên phải -->
        <androidx.cardview.widget.CardView
            android:id="@+id/vertical_card"
            android:layout_width="72dp"
            android:layout_height="match_parent"
            app:cardCornerRadius="12dp"
            app:cardElevation="3dp">
            <!-- Nội dung cho Card phải ở đây -->
        </androidx.cardview.widget.CardView>
    </LinearLayout>

    <!-- Header của Section, giữ nguyên cấu trúc lồng nhau theo yêu cầu -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/section_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="4dp"
        app:layout_constraintTop_toBottomOf="@id/top_cards_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- TextView "Xem tất cả" -->
        <TextView
            android:id="@+id/view_all_small"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Xem tất cả"
            android:textSize="12sp"
            android:textColor="?attr/colorPrimary"
            android:padding="4dp"
            android:background="?attr/selectableItemBackground"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Nhóm Title + Mũi tên -->
        <LinearLayout
            android:id="@+id/title_arrow_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/view_all_small"
            android:layout_marginTop="6dp">

            <TextView
                android:id="@+id/section_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Danh sách học phần"
                android:textSize="16sp"
                android:textStyle="bold"/>

            <ImageView
                android:id="@+id/chevron_right"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/baseline_arrow_forward_24"
                android:layout_marginStart="6dp"
                app:tint="?android:attr/textColorPrimary"
                tools:ignore="ContentDescription" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- RecyclerView cho danh sách -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_courses"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        app:layout_constraintTop_toBottomOf="@id/section_header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_course"
        tools:ignore="SpeakableTextPresentCheck" />
</androidx.constraintlayout.widget.ConstraintLayout>
